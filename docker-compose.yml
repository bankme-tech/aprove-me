version: "3"
services:
  # nginx:
  #   depends_on:
  #     - backend
  #     - frontend
  #   restart: always
  #   build:
  #     context: ./nginx
  #   ports:
  #     - "3050:80"
  backend:
    build:
      context: ./backend
      dockerfile: ./Dockerfile.development
    env_file:
      - ./.env
    # volumes:
    #   - /app/node_modules
    #   - ./backend:/app
  frontend:
    stdin_open: true
    build:
      context: ./frontend
      dockerfile: ./Dockerfile.development
    volumes:
      - /app/node_modules
      - ./frontend:/app
