version: '3.8'

services:
  app:
    image: node:20.7.0-alpine
    container_name: app
    working_dir: /app
    command: sh -c "npm run start:dev"
    build:
      context: .
    ports:
      - '${PORT:-3000}:3000'
    volumes:
      - .:/app