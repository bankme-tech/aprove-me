version: '3.8'
services:
  db:
    image: postgres:13
    environment:
      POSTGRES_USER: aproveme
      POSTGRES_PASSWORD: aproveme
      POSTGRES_DB: aproveme
    ports:
      - '5432:5432'
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data: